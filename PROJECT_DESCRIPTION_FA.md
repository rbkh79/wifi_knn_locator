# توضیحات کامل پروژه: اپلیکیشن موقعیت‌یابی داخلی با Wi-Fi و الگوریتم KNN

## 📋 مقدمه و اهداف پروژه

این پروژه شامل طراحی و پیاده‌سازی یک اپلیکیشن موبایل پیشرفته برای **موقعیت‌یابی داخلی (Indoor Localization)** با استفاده از سیگنال‌های Wi-Fi است. اپلیکیشن با بهره‌گیری از تکنیک **Wi-Fi Fingerprinting** و الگوریتم **K-Nearest Neighbors (KNN)** قادر به تخمین موقعیت کاربر در محیط‌های بسته (ساختمان‌ها، فضاهای داخلی) با دقت مناسب است.

**نکته مهم**: این سیستم به‌طور کامل مستقل از سنسورهای حرکتی (IMU، شتاب‌سنج، ژیروسکوپ) کار می‌کند و تنها از اطلاعات Wi-Fi (RSSI و MAC Address) استفاده می‌نماید. این ویژگی باعث می‌شود سیستم در تمام دستگاه‌های موبایل قابل اجرا باشد و نیازی به سخت‌افزارهای خاص نداشته باشد.

---

## 🎯 اهداف اصلی پروژه

1. **موقعیت‌یابی دقیق در محیط‌های بسته**: تخمین موقعیت کاربر با استفاده از الگوی سیگنال‌های Wi-Fi
2. **جمع‌آوری و ذخیره‌سازی داده**: ثبت سیستماتیک تمام اسکن‌های Wi-Fi و تخمین‌های موقعیت برای تحلیل‌های پژوهشی
3. **پیش‌بینی حرکت کاربر**: استفاده از مدل Markov برای پیش‌بینی مسیر حرکتی کاربر
4. **ارائه ابزارهای پژوهشی**: امکان Export داده‌ها و تحلیل آماری برای پژوهش‌های دانشگاهی

---

## 🏗️ معماری سیستم

### ساختار کلی

سیستم به‌صورت **ماژولار** طراحی شده است که امکان توسعه، تست و نگهداری آسان را فراهم می‌کند:

```
lib/
├── config.dart                 # تنظیمات و پارامترهای قابل تنظیم
├── data_model.dart             # مدل‌های داده (WifiReading, FingerprintEntry, LocationEstimate)
├── wifi_scanner.dart           # ماژول اسکن Wi-Fi (با استفاده از wifi_scan)
├── local_database.dart         # مدیریت پایگاه داده SQLite
├── knn_localization.dart       # پیاده‌سازی الگوریتم KNN
├── main.dart                   # رابط کاربری اصلی
├── services/
│   ├── fingerprint_service.dart         # مدیریت اثرانگشت‌ها (Training Mode)
│   ├── data_logger_service.dart        # ثبت سیستماتیک اسکن‌ها و تخمین‌ها
│   ├── movement_prediction_service.dart # مدل Markov برای پیش‌بینی حرکت
│   ├── location_service.dart           # مدیریت موقعیت GPS (اختیاری)
│   ├── fingerprint_validator.dart      # اعتبارسنجی کیفیت اثرانگشت‌ها
│   ├── data_export_service.dart        # Export داده‌ها به CSV/JSON
│   └── settings_service.dart           # مدیریت تنظیمات کاربر
└── utils/
    ├── privacy_utils.dart      # ابزارهای حریم خصوصی (هش MAC)
    └── rssi_filter.dart        # فیلتر نویز و پردازش RSSI
```

### تکنولوژی‌های استفاده شده

- **Flutter**: فریمورک توسعه اپلیکیشن موبایل (Android + iOS)
- **SQLite (sqflite)**: پایگاه داده محلی برای ذخیره‌سازی داده‌ها
- **wifi_scan**: پکیج اسکن Wi-Fi
- **geolocator**: مدیریت موقعیت GPS (اختیاری)
- **flutter_map**: نمایش نقشه و نقاط مرجع
- **crypto**: هش کردن MAC Address برای حریم خصوصی

---

## 🔬 الگوریتم‌های پیاده‌سازی شده

### 1. الگوریتم KNN (K-Nearest Neighbors)

#### اصول کار

1. **جمع‌آوری اثرانگشت‌ها (Training Phase)**:
   - در نقاط مرجع با مختصات شناخته‌شده، اسکن Wi-Fi انجام می‌شود
   - برای هر نقطه، لیستی از Access Pointها (AP) با RSSI و MAC Address ذخیره می‌شود
   - این داده‌ها در پایگاه داده SQLite به‌صورت دائمی ذخیره می‌شوند

2. **تخمین موقعیت (Online Phase)**:
   - هنگام اسکن جدید، بردار RSSI فعلی با تمام بردارهای اثرانگشت مقایسه می‌شود
   - فاصله اقلیدسی وزن‌دار محاسبه می‌شود
   - k همسایه نزدیک انتخاب می‌شوند
   - موقعیت تخمینی از میانگین وزن‌دار k همسایه محاسبه می‌شود

#### فرمول‌های ریاضی

**محاسبه فاصله اقلیدسی وزن‌دار**:
```
distance = √(Σ (RSSI_observed - RSSI_fingerprint)² × weight)
```

**وزن‌دهی RSSI** (برای بهبود دقت):
```
weight = 10^(-RSSI/10)
```
که در آن RSSI قوی‌تر (کمتر منفی) وزن بیشتری دارد.

**محاسبه موقعیت تخمینی**:
```
lat_estimated = Σ(lat_i × weight_i) / Σ(weight_i)
lon_estimated = Σ(lon_i × weight_i) / Σ(weight_i)
```
که در آن `weight_i = 1 / (distance_i + 1)`

**محاسبه ضریب اطمینان**:
```
confidence = 1 / (1 + average_distance / 100)
```

#### بهبودهای اعمال شده

- **فیلتر نویز**: استفاده از میانه RSSI برای مقاومت در برابر نویز
- **حذف APهای موقت**: فقط APهایی که در >70% اسکن‌ها ظاهر شده‌اند استفاده می‌شوند
- **وزن‌دهی RSSI**: APهای قوی‌تر تأثیر بیشتری در محاسبه دارند
- **Validation**: هنگام ذخیره اثرانگشت، 3 اسکن انجام می‌شود و فقط اگر همگرا باشند ذخیره می‌شود

### 2. مدل Markov برای پیش‌بینی حرکت

سیستم از یک **مدل Markov ساده** برای پیش‌بینی حرکت کاربر استفاده می‌کند:

1. **ساخت ماتریس انتقال**: از تاریخچه موقعیت‌های قبلی، احتمال گذار از یک ناحیه به ناحیه دیگر محاسبه می‌شود
2. **پیش‌بینی**: با استفاده از آخرین موقعیت، محتمل‌ترین ناحیه بعدی پیش‌بینی می‌شود

**فرمول**:
```
P(zone_next | zone_current) = count(zone_current → zone_next) / count(zone_current)
predicted_zone = argmax(P(zone_next | zone_current))
```

---

## 📊 ساختار پایگاه داده

پایگاه داده SQLite شامل 5 جدول اصلی است:

### 1. جدول `fingerprints`
ذخیره اطلاعات نقاط مرجع (Reference Points):
- `id`: شناسه یکتا
- `fingerprint_id`: شناسه اثرانگشت
- `latitude`, `longitude`: مختصات جغرافیایی
- `zone_label`: لیبل ناحیه (اختیاری)
- `created_at`: زمان ثبت
- `device_id`: شناسه دستگاه کاربر

### 2. جدول `access_points`
ذخیره اطلاعات APهای مربوط به هر اثرانگشت:
- `id`: شناسه یکتا
- `fingerprint_id`: ارجاع به fingerprint
- `bssid`: MAC Address نقطه دسترسی
- `rssi`: قدرت سیگنال (dBm)
- `frequency`: فرکانس (MHz)
- `ssid`: نام شبکه Wi-Fi

### 3. جدول `wifi_scans`
ثبت هر اسکن Wi-Fi:
- `id`: شناسه یکتا
- `device_id`: شناسه دستگاه
- `timestamp`: زمان اسکن

### 4. جدول `wifi_scan_readings`
ذخیره جزئیات هر اسکن:
- `id`: شناسه یکتا
- `scan_id`: ارجاع به wifi_scans
- `bssid`, `rssi`, `frequency`, `ssid`: اطلاعات AP

### 5. جدول `location_history`
تاریخچه تخمین‌های موقعیت:
- `id`: شناسه یکتا
- `device_id`: شناسه دستگاه
- `latitude`, `longitude`: مختصات تخمینی
- `zone_label`: ناحیه تخمینی
- `confidence`: ضریب اطمینان (0.0 تا 1.0)
- `timestamp`: زمان تخمین

---

## 🎨 رابط کاربری و قابلیت‌ها

### بخش‌های اصلی UI

1. **بخش موقعیت دستگاه (Device Location)**:
   - نمایش موقعیت GPS فعلی (در صورت فعال بودن)
   - دکمه "Refresh Location" برای به‌روزرسانی
   - نمایش مختصات (Latitude, Longitude)

2. **بخش اسکن Wi-Fi (Wi-Fi Scan)**:
   - نمایش شناسه هش‌شده دستگاه
   - دکمه "Start Wi-Fi Scan" برای شروع اسکن
   - نمایش لیست APهای مشاهده‌شده با RSSI و MAC Address (ماسک‌شده)
   - نمایش قدرت سیگنال با نمادهای بصری

3. **بخش نتایج سیگنال (Signal Results)**:
   - نمایش موقعیت تخمینی (Latitude, Longitude)
   - نمایش ناحیه تخمینی (Zone Label)
   - نمایش ضریب اطمینان (Confidence)
   - نمایش پیش‌بینی حرکت بعدی (Markov)
   - نمایش k همسایه نزدیک

4. **بخش نقشه (Map)**:
   - نمایش نقشه تعاملی با استفاده از OpenStreetMap
   - نمایش نقاط مرجع (Reference Points) به‌صورت مارکرهای رنگی
   - نمایش موقعیت GPS فعلی
   - نمایش موقعیت تخمینی KNN
   - امکان کلیک روی نقشه برای افزودن نقطه مرجع جدید

5. **بخش تنظیمات (Settings)**:
   - فعال/غیرفعال کردن استفاده از GPS
   - فعال/غیرفعال کردن Validation
   - تنظیم مقدار k برای الگوریتم KNN

6. **بخش حالت پژوهشگر (Researcher Mode)**:
   - نمایش آمار کامل (تعداد Fingerprintها، اسکن‌ها، تخمین‌ها)
   - نمایش میانگین Confidence
   - دکمه Export داده‌ها به CSV/JSON
   - امکان مشاهده تاریخچه کامل

### حالت آموزش (Training Mode)

در این حالت، کاربر می‌تواند اثرانگشت‌های جدید را به پایگاه داده اضافه کند:

1. **روش اول: Training Mode دستی**:
   - فعال کردن سوئیچ "Training Mode"
   - ایستادن در نقطه مرجع
   - انجام اسکن Wi-Fi
   - وارد کردن مختصات و لیبل ناحیه
   - ذخیره اثرانگشت

2. **روش دوم: کلیک روی نقشه**:
   - کلیک روی نقطه مورد نظر در نقشه
   - وارد کردن لیبل ناحیه (اختیاری)
   - تأیید - اسکن به‌صورت خودکار انجام می‌شود

### اعتبارسنجی داده‌ها (Validation)

هنگام ذخیره اثرانگشت (در صورت فعال بودن Validation):

1. **چند اسکن**: 3 بار اسکن انجام می‌شود
2. **بررسی همگرایی**: واریانس RSSI باید کمتر از 15 dBm باشد
3. **فیلتر APهای موقت**: فقط APهایی که در >70% اسکن‌ها ظاهر شده‌اند ذخیره می‌شوند
4. **بررسی ناهنجاری**: اگر نقطه جدید به یک نقطه موجود نزدیک است ولی RSSI بسیار متفاوت دارد، هشدار داده می‌شود

---

## 🔐 حریم خصوصی و امنیت

1. **هش کردن MAC Address دستگاه**:
   - MAC Address دستگاه کاربر با SHA-256 هش می‌شود
   - یک UUID یکتا برای هر دستگاه تولید و در SharedPreferences ذخیره می‌شود
   - این UUID برای شناسایی دستگاه در تمام جلسات استفاده می‌شود

2. **ماسک کردن MAC Addressها**:
   - در رابط کاربری، فقط 6 کاراکتر اول MAC Address نمایش داده می‌شود
   - بقیه با `X` جایگزین می‌شود
   - این کار شفافیت همراه با حریم خصوصی را فراهم می‌کند

3. **ذخیره‌سازی محلی**:
   - تمام داده‌ها به‌صورت محلی در دستگاه ذخیره می‌شوند
   - هیچ داده‌ای به سرور ارسال نمی‌شود (مگر اینکه در آینده اضافه شود)

---

## 📈 قابلیت‌های پژوهشی

### Export داده‌ها

سیستم امکان Export تمام داده‌ها را به دو فرمت فراهم می‌کند:

1. **CSV**: برای تحلیل در Excel یا Python
2. **JSON**: برای پردازش برنامه‌نویسی

**داده‌های Export شده شامل**:
- تمام اثرانگشت‌ها (Fingerprints)
- تمام اسکن‌های Wi-Fi (Wi-Fi Scans)
- تمام تخمین‌های موقعیت (Location History)

### آمار و معیارهای ارزیابی

سیستم آمار زیر را ارائه می‌دهد:
- تعداد Fingerprintها
- تعداد اسکن‌های Wi-Fi
- تعداد تخمین‌های موقعیت
- تعداد BSSIDهای یکتا
- میانگین Confidence

### معیارهای ارزیابی دقت

برای تحلیل دقت سیستم، می‌توان از معیارهای زیر استفاده کرد:

1. **خطای مکانی (Positioning Error)**:
   ```
   error = √((lat_estimated - lat_actual)² + (lon_estimated - lon_actual)²) × 111,000
   ```
   (ضریب 111,000 برای تبدیل درجه به متر)

2. **ضریب اطمینان (Confidence Score)**: مقدار بین 0.0 تا 1.0

3. **دقت ناحیه (Zone Accuracy)**:
   ```
   zone_accuracy = (تعداد_تخمین_های_صحیح / تعداد_کل_تخمین‌ها) × 100%
   ```

---

## 🧪 تست‌ها و اعتبارسنجی

### تست‌های واحد (Unit Tests)

- **knn_localization_test.dart**: تست الگوریتم KNN با داده‌های ساختگی
- **wifi_scanner_test.dart**: تست ماژول اسکن Wi-Fi با داده‌های شبیه‌سازی شده

### تست‌های یکپارچه (Integration Tests)

- **integration_test.dart**: تست کامل جریان داده از اسکن تا تخمین موقعیت

### اجرای تست‌ها

```bash
flutter test
```

---

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

- Flutter SDK (>=3.0.0)
- Dart SDK (>=3.0.0)
- Android SDK (برای Android)
- Xcode (برای iOS)

### نصب وابستگی‌ها

```bash
flutter pub get
```

### اجرای اپلیکیشن

```bash
# Android
flutter run

# iOS
flutter run -d ios
```

### ساخت فایل APK/AAB

```bash
# ساخت APK
flutter build apk --release

# ساخت AAB (برای Google Play)
flutter build appbundle --release
```

---

## 📚 نتایج و دستاوردها

### ویژگی‌های کلیدی

✅ **موقعیت‌یابی دقیق**: استفاده از الگوریتم KNN با وزن‌دهی RSSI برای بهبود دقت

✅ **جمع‌آوری داده سیستماتیک**: ثبت کامل تمام اسکن‌ها و تخمین‌ها در پایگاه داده

✅ **پیش‌بینی حرکت**: استفاده از مدل Markov برای پیش‌بینی مسیر حرکتی

✅ **اعتبارسنجی خودکار**: بررسی کیفیت داده‌ها قبل از ذخیره‌سازی

✅ **ابزارهای پژوهشی**: امکان Export داده‌ها و تحلیل آماری

✅ **حریم خصوصی**: هش و ماسک کردن MAC Addressها

✅ **رابط کاربری کاربرپسند**: طراحی شفاف و قابل فهم برای پژوهشگران

### کاربردهای عملی

1. **پژوهش‌های دانشگاهی**: تحلیل دقت الگوریتم‌های موقعیت‌یابی
2. **نقشه‌برداری داخلی**: ایجاد نقشه Wi-Fi از ساختمان‌ها
3. **مدیریت منابع**: ردیابی موقعیت در محیط‌های بسته
4. **نقشه‌برداری ناوبری**: راهنمایی در ساختمان‌های بزرگ

---

## 🔮 توسعه‌های آینده

1. **الگوریتم‌های پیشرفته‌تر**: 
   - Gaussian Process برای بهبود دقت
   - Neural Networks برای یادگیری الگوهای پیچیده

2. **بهبود دقت**:
   - استفاده از چند فرکانس (2.4 GHz و 5 GHz)
   - ترکیب با داده‌های BLE (Bluetooth Low Energy)

3. **ویژگی‌های کاربردی**:
   - نقشه‌برداری خودکار
   - هشدار در صورت خروج از مسیر
   - بهینه‌سازی مسیر

---

## 📝 نتیجه‌گیری

این اپلیکیشن یک سیستم کامل و قابل استفاده برای موقعیت‌یابی داخلی با Wi-Fi است که تمام نیازهای یک پروژه پژوهشی را برآورده می‌کند. معماری ماژولار، امکان توسعه و بهبود را فراهم می‌کند و ابزارهای پژوهشی تعبیه‌شده، تحلیل و ارزیابی را تسهیل می‌نماید.

سیستم به‌طور کامل مستقل از سنسورهای حرکتی کار می‌کند و تنها از اطلاعات Wi-Fi استفاده می‌نماید که این ویژگی باعث می‌شود در تمام دستگاه‌های موبایل قابل اجرا باشد.

---

**تاریخ تهیه**: 1403  
**نسخه**: 1.0  
**توسعه‌دهنده**: [نام شما]  
**دانشگاه**: دانشگاه فردوسی مشهد  
**دانشکده**: دانشکده مهندسی


